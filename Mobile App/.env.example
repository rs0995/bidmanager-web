# Only needed for a production build hosted on a DIFFERENT origin than the
# backend (e.g. a static host, not same-origin behind the VM's Caddy).
# Leave unset for LAN dev testing — the Vite dev proxy handles it instead.
VITE_API_BASE_URL=https://161.118.170.233.sslip.io
