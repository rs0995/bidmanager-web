# Public API hostname for packaged Client UI builds.
# OCI Ampere VM (migrated off Google Cloud Run 2026-08-28). Matches BIDMANAGER_DOMAIN
# in deploy/oci/.env and PRODUCTION_API_BASE in Server UI/BidManagerControl.jsx.
# IP-pinned sslip.io hostname until a real domain is put in front.
VITE_API_BASE_URL=https://161.118.170.233.sslip.io
