import { View, Text } from "react-native";

// Placeholder -- replaced with the full Projects screen in Phase 6.
export default function ProjectsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-bg">
      <Text className="text-text text-base font-semibold">Projects</Text>
    </View>
  );
}
