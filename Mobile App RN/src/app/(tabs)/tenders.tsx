import { View, Text } from "react-native";

// Placeholder -- replaced with the full Tenders screen in Phase 6.
export default function TendersScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-bg">
      <Text className="text-text text-base font-semibold">Tenders</Text>
    </View>
  );
}
