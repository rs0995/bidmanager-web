import { View, Text } from "react-native";

// Placeholder -- replaced with the full Tender Detail screen in Phase 6.
export default function TenderDetailScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-bg">
      <Text className="text-text text-base font-semibold">Tender Detail</Text>
    </View>
  );
}
